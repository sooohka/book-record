function useSearchState() {
  // TODO:훅 작성
  // const [searchQuery, setSearchQuery] = useRecoilState(searchQueryState);
  // const [searchPage, setSearchPage] = useRecoilState(searchPageState);

  return {};
}
export default useSearchState;
