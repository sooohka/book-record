import styled from "styled-components";

const Content = styled.main`
  flex-grow: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
`;

const S = {
  Content,
};

export default S;
