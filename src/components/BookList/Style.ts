import styled from "styled-components";

const Container = styled.ul`
  flex-direction: column;
  gap: 1rem;

  display: flex;
  overflow: scroll;

  padding: 1rem;
`;
const S = { Container };

export default S;
